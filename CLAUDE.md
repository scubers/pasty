# pasty2 Development Guidelines

## FIRST: MUST READ AGENTS.md

Auto-generated from all feature plans. Last updated: 2026-02-07

## Active Technologies
- Local SQLite database for history + local files for image assets (paths stored as relative references) (002-clipboard-main-panel)
- C++17 (Core), Swift 5.9 (macOS shell) + Cocoa.framework (AppKit UI/system integration), system SQLite (local storage), XcodeGen (project generation), Combine (macOS reactive data flow) (002-clipboard-main-panel)
- C++17 (Core), Swift 5.9 (macOS shell) + Cocoa.framework (AppKit), SwiftUI (system), Combine (system), XcodeGen, system SQLite (002-clipboard-main-panel)
- Local SQLite database for history + local files for image assets (relative paths) (002-clipboard-main-panel)

- C++17 (Core), Swift 5.9 (macOS shell) + Cocoa.framework (macOS UI/system integration), system SQLite (local storage), XcodeGen (project generation) (001-clipboard-history)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for C++17 (Core), Swift 5.9 (macOS shell)

## Code Style

C++17 (Core), Swift 5.9 (macOS shell): Follow standard conventions

## Recent Changes
- 002-clipboard-main-panel: Added C++17 (Core), Swift 5.9 (macOS shell) + Cocoa.framework (AppKit), SwiftUI (system), Combine (system), XcodeGen, system SQLite
- 002-clipboard-main-panel: Added C++17 (Core), Swift 5.9 (macOS shell) + Cocoa.framework (AppKit UI/system integration), system SQLite (local storage), XcodeGen (project generation), Combine (macOS reactive data flow)
- 002-clipboard-main-panel: Added C++17 (Core), Swift 5.9 (macOS shell) + Cocoa.framework (macOS UI/system integration), system SQLite (local storage), XcodeGen (project generation)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
